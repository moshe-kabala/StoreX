import "jest";

describe("filter wrapper", () => {
  test("create boolean filter - single", async () => {});

  const request = {
      where: [
          {
              key: "filter1",
              type: "boolean",
              value: true,
              operator: "=",
              path: ""

          }
      ]
  }

  const expected = {
      
  }


});
